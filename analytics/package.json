{
  "name": "lvb-analytics-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "hash-password": "node scripts/hash-admin-password.mjs",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "dev": "wrangler dev --local",
    "qa:local": "node scripts/run-local-qa.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "4.127.0"
  }
}
