# Lv.B Workspace Router

- 작업 루트는 `E:\Codex\LvB\Homepage`다.
- 시작 시 이 문서 → `docs/README.md` → `docs/CURRENT.md` 순서로 읽고, `docs/README.md`에서 작업에 필요한 담당 문서만 선택한다.
- 신규 홈페이지 구현은 `site/`에서만 수행한다. `legacy-site/`는 명시적 요청 없이는 수정하지 않는 읽기 전용 보관 영역이다.
- `/privacy` 또는 보호 대상에 영향이 있을 수 있는 작업은 `docs/LOCKED.md`를 먼저 읽는다.
- News 항목·라우트·노출 조건은 `docs/NEWS.md`를 따른다.
- 완료 전 공통 검증은 `docs/VALIDATION.md`를 따른다.
- 프로젝트 문서는 `docs/`, 입력·검토 자료는 `references/`, Codex 설정은 루트 `.codex/config.toml`에서 관리한다.
