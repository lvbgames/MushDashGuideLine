# Current

- 전체 페이지 구현 상태: 기존 정규 28개 locale 페이지, 네 언어 Privacy route와 브랜드 404를 유지한다. Privacy 네 언어의 최종 수정일과 시행일은 `2026-08-03`으로 확정했으며, 첫 언급에 `Epic Online Services(EOS)`, 이후에는 `EOS`를 사용한다. `E:\MushDash` 현재 작업 트리에서 확인한 튜토리얼·표시 이름·아이콘/이름표/아바타·재화·인벤토리·Infinity Tower·주간 도전 ID/상태·중복 구매 방지 거래 식별자 범주, 계정 식별자와 payload 구분, 로컬 설정 분리와 자동 삭제 부재를 같은 의미로 반영했다. 내부 요청 절차는 `PRIVACY_REQUEST_RUNBOOK.md`에만 둔다.
- QA 결과: 게임 코드는 읽기 전용으로 재확인했으며 UserCloud 정상 key 6개, 로그인 후 read, dirty data write, 성공 후 read-back, failure 시 강제 upload/delete 없음과 프로젝트 delete 호출 0건이 감사 문서와 일치했다. `npm ci`는 301개 package·취약점 0건, Astro check는 102개 파일 0 errors·0 warnings·0 hints, 정적 build는 33 HTML, sitemap은 공개 URL 28개, `prepare-production.ps1`은 통과했다. Privacy 4 route·각 19 section과 공개 HTML의 `EIK` 0건, broken link·missing asset 0건, 320·390·768·1440px 16개 조합의 가로 overflow·일반 텍스트 clipping·깨진 이미지 0건, 모바일 메뉴 Escape·초점 복귀와 console error·warning 0건을 확인했다. 운영 `/robots.txt`는 일반·Yeti 요청 모두 200 `text/plain`으로 정상이며, 로컬 원본에는 Yeti 명시 허용을 추가하고 public·dist byte 일치·BOM·본문 검증을 강화했다.
- 배포 전 확인 항목: 사용자가 `2026-08-03` 운영 배포와 시행일을 승인했다. 현재 감사 소스와 실제 Steam·Epic 공개 package의 provenance, Steam·Epic별 안전한 계정 소유 확인, 제품별 UserCloud 열람·정정·삭제 권한과 조직 단위 `Delete User` 영향, 요청 처리 기록 보유기간, transaction identifier load 비활성·runtime log 정책, 로컬 `PS` slot packaged round-trip, 외부 사업자 법적 지위·국외 처리 유형과 최종 법률 문구는 배포 후에도 법률·운영 검토 권고로 유지한다.
- 다음 권장 작업: robots 변경을 별도 승인 후 배포하고 운영 curl 재검증 뒤 네이버 서치어드바이저에서 robots.txt 수집을 다시 요청한다. 별도 MushDash 개발 작업에서는 로그인한 이용자의 정상 gameplay·transaction 파일 여섯 개만 삭제하는 확인 UI, 재열거, 로컬 유지/초기화 정책, 부분 실패·재시도와 Steam·Epic 회귀를 구현·검증한다.
